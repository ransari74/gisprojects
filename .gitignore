# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.local
*.local

# Data downloads
data/raw/
data/processed/
# Resumable per-request cache for etl/real_data.py's harvest step -- not the
# bundled output (etl/real_boundaries/), which IS committed.
data/real_cache/
*.osm.pbf
*.gpkg
*.tif
*.zip

# Repo-level tooling (playwright for the screenshot smoke test)
/node_modules/
